import React from 'react'
import ReactDOM from 'react-dom'
import './index.css'
import SortSampleApp from './SortSampleApp'

ReactDOM.render(
  <React.StrictMode>
    <SortSampleApp />
  </React.StrictMode>,
  document.getElementById('root')
)
